version: '2'

services:
  gitlab:
    restart: always
    image: gitlab/gitlab-ce:latest
    ports:
    - "10080:80"
    - "10022:22"
    volumes:
        #- /srv/gitlab/config:/etc/gitlab
        #- /srv/gitlab/logs:/var/log/gitlab
        #- /srv/gitlab/data:/var/opt/gitlab


        - /mnt/sda1/srv/gitlab/config:/etc/gitlab
        - /mnt/sda1/srv/gitlab/logs:/var/log/gitlab
        - /mnt/sda1/srv/gitlab/data:/var/opt/gitlab

    environment:
    - DEBUG=false
    - hostname=git.louislabs.com
